{
  "enabledPlugins": {
    "document-skills@anthropic-agent-skills": true
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "py -c \"import json,sys,subprocess as sp; d=json.load(sys.stdin); f=(d.get('tool_input') or {}).get('file_path') or (d.get('tool_response') or {}).get('filePath'); sp.run(['py','-m','black','-q',f]) if f and f.endswith('.py') else None\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
